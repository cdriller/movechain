# Sepolia config — loaded by `vite --mode sepolia` (npm run sepolia).
# Committed on purpose: contains only public values (no secrets).
# To use a private RPC (Alchemy/Infura), override VITE_SEPOLIA_RPC_URL in
# frontend/.env.sepolia.local (gitignored) — that file takes precedence.
VITE_CHAIN=sepolia
VITE_MOVECHAIN_ADDRESS=0xE5D4B600fA9BE3e00dABcef13F30b60691569D34
VITE_SEPOLIA_RPC_URL=https://ethereum-sepolia-rpc.publicnode.com
